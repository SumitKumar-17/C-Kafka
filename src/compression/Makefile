# This Makefile is intentionally left blank for now.
# It serves as a placeholder for the main build system.
# The compression object files are built directly by other modules that need them.

all:
clean:
run: